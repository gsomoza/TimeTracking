{{#if withLayout}}
    <header class="navbar navbar-inverse primary" role="navigation">
        <div class="container">
            <a class="sr-only" href="#content"> … </a>
            <div class="container-fluid">
                <div class="navbar-header">
                    <a href="#" class="navbar-brand">Time Tracker</a>
                    <button class="navbar-toggle collapsed" type="button" data-toggle="collapse" data-target="#navbar-main">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                </div>

                <div class="collapse navbar-collapse ag-navbar-collapse" id="navbar-main">
                    <ul class="nav navbar-nav">
                        {{#if session.isAuthenticated}}
                            <li>{{#link-to 'worklogs' class="btn navbar-btn navbar-left"}}Worklogs{{/link-to}}</li>
                        {{/if}}
                    </ul>
                    <ul class="nav navbar-nav navbar-right">
                        {{# if session.isAuthenticated}}
                            <li>{{#link-to 'user' session.account_id class="btn navbar-btn navbar-left"}}<span class="glyphicon glyphicon-user"></span> My Account{{/link-to}}</li>
                            <li><a {{action 'invalidateSession'}} class="btn navbar-btn navbar-left"><span class="glyphicon glyphicon-log-out"></span> Logout</a></li>
                        {{else}}
                            <li>{{#link-to 'login' class="btn navbar-btn navbar-left" }}Login{{/link-to}}</li>
                        {{/if}}
                    </ul>
                </div>
            </div>
        </div>
    </header>

    <div class="container">
        {{bs-notifications}}

        {{outlet}}
        {{outlet modal}}
    </div> <!-- /container -->

    <footer>
        <hr>
        <div class="container center-block">
            <p>&copy; {{year}} by <a href="http://somoza.me">Gabriel Somoza</a>. All rights reserved.</p>
        </div>
    </footer>
    {{log record}}
{{else}}
    {{outlet}}
{{/if}}
